
function flatten(array) {
	// flatten array if it has arrays in it.
}

/**
 * @brief 
 * @details 
 * 
 * @param array
 * @param width
 * @param height
 */
function matrix(array, width_height) {
	var self = this;
	self.array = array;
}

